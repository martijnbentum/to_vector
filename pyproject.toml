[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "to-vector"
version = "0.1.0"
dependencies = ["librosa","transformers","torch","torchaudio"]
description = "A tool to extract hidden states from models."
